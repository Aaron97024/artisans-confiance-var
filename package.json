{
  "name": "artisans-confiance-var",
  "private": true,
  "version": "1.0.0",
  "description": "Association des Artisans de Confiance — site statique",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "npm run build && wrangler pages deploy .cf-pages --project-name artisans-confiance-var"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
